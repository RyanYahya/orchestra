---
name: simplify
description: Cleanup-only review for reuse, simplification, efficiency, and altitude; then apply behavior-preserving fixes
argument-hint: '[target]'
---

Read and follow the instructions in `.agents/skills/simplify/SKILL.md`.

If that file is missing, run the same cleanup-only pass as Claude Code's `/simplify`: four parallel review agents for reuse, simplification, efficiency, and altitude, then apply only behavior-preserving fixes.

Task / arguments: $ARGUMENTS
